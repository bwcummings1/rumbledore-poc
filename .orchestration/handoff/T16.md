# T16 Handoff - Real ESPN 95050 Dev-DB Screenshots

## What landed

- Populated and verified the shared dev DB used by the running app, not an isolated throwaway DB.
- The real ESPN provider identity is `espn` / `95050`.
- Current shared row resolved by provider identity:
  - `NHS Alumni Annual`
  - `f8334edb-7a20-4138-aa7a-65c0b5285886`
- The task-note UUID `466e2035-6c78-451a-b517-bcc5accae436` was not present in this shared DB by the time T16 ran, so T16 tooling resolves by provider identity and reports the current UUID.
- The shared DB now has:
  - 16 pushed canonical seasons, 2011-2026.
  - 24,433 `fantasy_roster_entries`.
  - 2,968 `fantasy_draft_picks`.
  - 0 placeholder persons.
  - 0 `unknown` decoded player positions, pro teams, or roster slots.
  - `provider_code_decoding` PASS.
  - Confirmed era `12-team era (2013-2014)`.

## Repeatable Population Check

Run from `/home/ubuntu/rmbl-T16`:

```bash
PATH=/usr/bin:$PATH pnpm exec tsx scripts/verify-t16-real-league-population.ts
```

The verifier intentionally targets the default dev DB from `.env.local`/env schema:

```text
postgres://rumbledore:rumbledore@localhost:5440/rumbledore
```

It does not print ESPN cookies or secret values. It reuses the existing import harness path:

- `syncCurrentLeague`
- `importLeagueHistory`
- `recomputeLeagueStatistics`
- curation checkpoint/push bootstrap when pushed seasons are missing
- verification summary appended/replaced at `.orchestration/import-summary.md` under `T16 real-league population`

## Screenshot Session

Run after the population verifier:

```bash
T16_REAL_SCREENSHOTS=1 PATH=/usr/bin:$PATH pnpm exec playwright test e2e/real-95050-screenshots.spec.ts
```

Output root:

```text
docs/screenshots/real-95050/
```

Captured per viewport (`mobile`, `tablet`, `desktop`):

- `01-league-home.png`
- `02-press-front.png`
- `03-data-book-people.png`
- `04-data-book-settings.png`
- `05-data-book-weeks-roster-2012-wk8.png`
- `06-edit-ledger.png`
- `07-records.png`

The fixture baseline under `docs/screenshots/{desktop,tablet,mobile}/` is not touched.

## Auth and Access Setup

`e2e/real-95050-screenshots.spec.ts` creates a temporary Better Auth email user through the app auth API, then grants access directly in the shared DB:

- Inserts a `members` row for the real league organization with role `data_steward`.
- Updates the active session's `activeOrganizationId` to the real league id.
- Resolves the target league by `leagues.provider = 'espn'` and `provider_league_id = '95050'`.
- Selects a populated roster sample from 2012 week 8 containing Luke Kuechly.
- Deletes the temporary user in `afterAll`.

This mirrors the app's auth-plane membership model while keeping all league data reads inside normal route/RLS paths.

## Screenshot Verification

The final screenshot run passed and `/tmp/t16-real-screenshots.log` had duplicate-key grep = `0`.

Manual spot-checks:

- People view shows real owner names including `bradwcummings`, `truman1109`, and `w hardy`; no `Fixture Manager`.
- Weeks roster screenshot shows `W8 / MONROE_REBS`, `Team DOMTINATION`, and Luke Kuechly decoded as `DL / CAR / active` in slot `LB`.
- Records page shows real standings/records, pushed canonical seasons, and the `12-team era (2013-2014)` lens chip.
- League home and press pages are scoped to `NHS Alumni Annual`, not the fixture.

## Code Notes

- Added `scripts/verify-t16-real-league-population.ts` for repeatable dev-DB verification.
- Added `e2e/real-95050-screenshots.spec.ts` for gated real-league screenshot capture.
- Narrowly fixed `data_edit_ledger_completeness` in `src/stats/engine.ts` so valid curation checkpoint/push ledger markers are recognized by integrity checks. Without this, the T16 baseline push could create legitimate marker rows that the integrity check incorrectly flagged as missing targets.
- Added a focused assertion to `src/stats/engine.test.ts` covering curation checkpoint and push ledger targets.

## Validation

- `PATH=/usr/bin:$PATH pnpm exec tsx scripts/verify-t16-real-league-population.ts`
- `T16_REAL_SCREENSHOTS=1 PATH=/usr/bin:$PATH pnpm exec playwright test e2e/real-95050-screenshots.spec.ts`
- `PATH=/usr/bin:$PATH pnpm exec vitest run src/stats/engine.test.ts`
- `PATH=/usr/bin:$PATH pnpm typecheck`
- `PATH=/usr/bin:$PATH pnpm lint`

Build, secret scan, and UBS were run before handoff completion.
